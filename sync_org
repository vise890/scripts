#!/bin/bash
set -euo pipefail
IFS=$'\n\t'

export GIT_COMMITTER_NAME="cRoN"
export GIT_COMMITTER_EMAIL="cRoN@cron.org"

export GIT_AUTHOR_NAME="cRoN"
export GIT_AUTHOR_EMAIL="cRoN@cron.org"

(
  cd "$HOME/org" || exit

  git pull || true
  git push origin master || true

  git add .
  git commit -m 'sync'
  git push origin master
)
