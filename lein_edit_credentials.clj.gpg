#!/bin/bash
set -euo pipefail
IFS=$'\n\t'

cred_file="~/.lein/credentials.clj.gpg"
temp_file="~/.lein/credentials.clj"

gpg --decrypt "$cred_file" > "$temp_file"

$EDITOR "$temp_file"

gpg --default-recipient-self -e "$temp_file" > "$cred_file"
rm "$temp_file"
